# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES - Example Template
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to git!

# -----------------------------------------------------------------------------
# NODE ENVIRONMENT
# -----------------------------------------------------------------------------
NODE_ENV=production  # Use 'development' for local dev

# -----------------------------------------------------------------------------
# DATABASE CONNECTION
# -----------------------------------------------------------------------------
# NeonDB PostgreSQL connection string
# Get this from: https://console.neon.tech/app/projects
# REQUIRED: Must include sslmode=require&channel_binding=require
DATABASE_URL=postgresql://username:password@host.neon.tech/database?sslmode=require&channel_binding=require

# -----------------------------------------------------------------------------
# BETTER AUTH CONFIGURATION
# -----------------------------------------------------------------------------
# Generate a secure secret with: openssl rand -base64 32
# REQUIRED: Minimum 32 characters
BETTER_AUTH_SECRET=your-minimum-32-character-secret-key-here-replace-this

# The URL where your frontend is accessible
# For local development: http://localhost:3000
# For production: https://yourdomain.com
BETTER_AUTH_URL=http://localhost:3000

# Comma-separated list of allowed origins for CORS
# REQUIRED: Must include your frontend URL
TRUSTED_ORIGINS=http://localhost:3000,http://localhost:8000

# -----------------------------------------------------------------------------
# BACKEND API CONFIGURATION
# -----------------------------------------------------------------------------
# URL where your FastAPI backend is running
# This is used by Next.js for server-side API calls
NEXT_PUBLIC_API_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# EXTERNAL API KEYS
# -----------------------------------------------------------------------------
# OpenAI API Key (for AI chat features)
# Get this from: https://platform.openai.com/api-keys
# OPTIONAL: Only needed if using AI chat features
OPENAI_API_KEY=sk-your-openai-api-key-here

# -----------------------------------------------------------------------------
# NEXT.JS CONFIGURATION
# -----------------------------------------------------------------------------
# Enable Next.js standalone output for Docker
# This is set in next.config.js, no need to change
# OUTPUT_MODE=standalone

# -----------------------------------------------------------------------------
# DEVELOPMENT-ONLY VARIABLES
# -----------------------------------------------------------------------------
# Uncomment these for local development debugging
# DEBUG=true
# LOG_LEVEL=debug

# =============================================================================
# GENERATION COMMANDS
# =============================================================================
# 
# Generate BETTER_AUTH_SECRET:
#   Windows (PowerShell): [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
#   Mac/Linux: openssl rand -base64 32
#
# Test database connection:
#   psql "$DATABASE_URL"
#
# =============================================================================
